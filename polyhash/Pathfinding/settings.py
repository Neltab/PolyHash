global lines #lines of the grid
lines = 4
global columns #columns of the grid
columns = 5
global grid #Grille globale contenant les points de montage en tant que 1 et le reste a 0
global nodeGrid #Grille de nodes utilisée dans A*   Elle n'est initialisée qu'une seule fois dans le main et n'est pas modifiée ensuite.